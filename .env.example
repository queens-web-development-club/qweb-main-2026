VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key

# Every VITE_ variable is published in the browser bundle. The service-role key
# used by scripts/upload-sponsor-logos.mjs does not belong in this file or in
# .env.local; pass it on the command line or from a secret manager.
